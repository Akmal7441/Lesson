extends layouts/layout
block content 
    h1 Products 
    .ProductNew 
        each product in server
            .product
                img(src= product.photo, alt="")
                h2= product.name
                h6= product.id

